//injects the database inoformation into the called scope 'this'
(function() {
    function RoomCtrl() {
 
        
    }
    
    angular
        .module('blocChat')
        .controller('RoomCtrl', [RoomCtrl]);
})();